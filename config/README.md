# Config 模块

## 负责什么
- 负责统一管理配置项
- 负责读取环境变量、服务地址、数据库配置、TG 配置、模型配置
- 负责向其他模块提供统一配置对象

## 不负责什么
- 不负责业务流程控制
- 不负责分析判断
- 不负责知识内容本身的维护

## 输入 / 输出
- **输入**: 环境变量、配置文件、运行参数
- **输出**: 统一配置对象、配置读取结果

## 子模块说明
- `gray-release.js` - 灰度发布配置
