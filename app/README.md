# App 模块

## 负责什么
- 负责应用层启动与整体装配
- 负责把配置、服务、适配器、运行时对象组织起来
- 负责系统主入口的承接

## 不负责什么
- 不负责具体业务判断
- 不负责直接承载训练规则或质检规则
- 不负责分析引擎内部决策

## 输入 / 输出
- **输入**: 启动参数、系统配置、依赖模块
- **输出**: 可运行应用实例、服务启动结果、初始化完成状态

## 子模块说明
- `telegram/commands.js` - Telegram 命令解析器
- `telegram/formatter.js` - 消息格式化工具
